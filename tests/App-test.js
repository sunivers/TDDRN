/**
 * @format
 */

import 'react-native';
import React from 'react';
import App from '../App';
import { Item } from 'react-native/Libraries/Components/Picker/Picker';

describe('Jest', () => {
  it('is it working?', () => {
    const a = 1;
    expect(a + 1).toBe(2);
  })
})
